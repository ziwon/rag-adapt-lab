mode: enabled
lexical_groundedness:
  claim_support_threshold: 0.5
citation_metrics: false
judge:
  kind: disabled
  strict: false
  timeout_seconds: 30
  max_retries: 2
  cache: true
  minimum_metric_coverage: 0.8
  minimum_paired_examples: 30
